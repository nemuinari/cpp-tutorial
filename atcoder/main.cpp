#include <bits/stdc++.h>
using namespace std;

int main() {
    std::string A;
    std::string s;
    std::string C;

    std::cin >> A >> s >> C; 
    std::cout << A.at(0) << s.at(0) << C.at(0);

    return 0;
}
// A - AtCoder *** Contes 
